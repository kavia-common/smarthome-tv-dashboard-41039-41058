<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Mark touchscreen as optional for TV devices -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <!-- Optional: declare Leanback feature (not required to install on non-TV devices) -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <application
        android:label="@string/app_name"
        android:banner="@mipmap/ic_launcher"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.SmartHomeTV" >

        <activity
            android:name="org.example.app.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- Keep phone/tablet launcher for flexibility -->
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- Add Leanback launcher for Android TV home -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
